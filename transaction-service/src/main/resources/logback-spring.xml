<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console Appender for local development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- JSON Appender for Docker/Production -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <customFields>{"service":"transaction-service"}</customFields>
        </encoder>
    </appender>

    <!-- Local profile: plain text logs -->
    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.example.transaction" level="DEBUG"/>
    </springProfile>

    <!-- Docker profile: JSON logs for Loki -->
    <springProfile name="docker">
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
        <logger name="com.example.transaction" level="DEBUG"/>
    </springProfile>

    <!-- Test profile: minimal logging -->
    <springProfile name="test">
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.example.transaction" level="INFO"/>
    </springProfile>
</configuration>